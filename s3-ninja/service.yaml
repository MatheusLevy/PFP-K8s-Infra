apiVersion: v1
kind: Service
metadata:
  name: s3-ninja
  namespace: s3-ninja
  labels:
    app: s3-ninja
spec:
  type: NodePort
  selector:
    app: s3-ninja
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30944
      protocol: TCP
      name: http
